// Section C NO1
let a = 10;
let b = "10";

console.log(a == b);
console.log(a === b);

/**the double equals to represents loose equality, which means it only cares about the value of the variable, not the type
 * the triple equals to on the other hand represents strict equality, which means it checks both the value and data type.*/

// NO2
let score = 75;
console.log(score >= 50);
